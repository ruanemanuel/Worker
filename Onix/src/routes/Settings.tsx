import { ReactElement } from "react";
import SettingsOption from "../components/Settings/main";

const Settings = (): ReactElement => {
  return (
    <div>
      <h1>Configurações</h1>
      {/* Configurações */}
      <div></div>
    </div>
  );
};

export default Settings;
